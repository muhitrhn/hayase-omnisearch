{
  "name": "hayase-omnisearch",
  "version": "1.0.0",
  "description": "Unified Hayase extension — ID-based anime search across SeaDex, AnimeTosho, and SubsPlease",
  "private": true,
  "type": "module",
  "main": "index.json",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.0.0"
  }
}
